# Replicate: used for "Paste from CIM" extraction (Gemini). Get token at https://replicate.com/account/api-tokens
# Optional: app works without it (manual entry only).
REPLICATE_API_TOKEN=your_replicate_token_here
